# REST API

- 리소스 생성
    - POST / Create
      - POST `/todos`
- 리소스 조회
    - GET / Read
      - GET `/todos`
      - GET `/todos/:id`
- 리소스 수정
    - PUT(전체) / Update
        - PUT `/todos/:id`
    - PATCH(부분) / Update
- 리소스 삭제
    - DELETE / Delete
        - DELETE `/todos/:id`
